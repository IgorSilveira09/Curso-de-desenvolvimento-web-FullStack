const imprimirResultado = function (nota){
    switch (Math.floor (nota)){

        case 10:
        case 9:
            break
            //o break fecha a logica e não permite que execute tudo de uma vez
    }
}